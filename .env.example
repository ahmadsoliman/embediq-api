# Database configuration
POSTGRES_USER=embediq
POSTGRES_PASSWORD=devpassword
POSTGRES_DB=embediq

# Auth0 configuration
AUTH0_DOMAIN=dev-embediq.us.auth0.com
AUTH0_API_AUDIENCE=https://api.embediq.dev

# Application paths
DATA_DIR=/data/embediq/users

# LLM configuration (for LightRAG)
OPENAI_API_KEY=your_openai_api_key_here

# Optional advanced database configuration
# Uncomment and modify if needed for the vector and AGE extensions
# POSTGRES_INITDB_ARGS="--data-checksums"
# POSTGRES_HOST_AUTH_METHOD=trust 